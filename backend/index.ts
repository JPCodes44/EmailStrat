/**
 * Backend entry point.
 *
 * The Convex function API (`api`) and its types are generated into
 * `convex/_generated/` by `bunx convex dev`. Once generated, re-export it here
 * so frontend code can import the typed API surface from `@emailstrat/backend`:
 *
 *   export { api } from './convex/_generated/api';
 *
 * It is intentionally left commented until the deployment has been provisioned,
 * since `_generated/` does not exist on a fresh checkout.
 */
export {};
